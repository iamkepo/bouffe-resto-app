export const parseAction = index => (
  {
  type: 'PARSE',
    payload: index,
  }
);
export const panierAction = index => (
  {
    type: 'PANIER',
    payload: index,
  }
);
export const favorieAction = index => (
  {
    type: 'FAVORIE',
    payload: index,
  }
);
export const _panierAction = index => (
  {
    type: '_PANIER',
    payload: index,
  }
);

export const listAction = index => (
  {
    type: 'LIST',
    payload: index,
  }
);

export const userAction = index => (
  {
    type: 'USER',
    payload: index,
  }
);

export const restaurantAction = index => (
  {
    type: 'RESTO',
    payload: index,
  }
);

export const platAction = index => (
  {
    type: 'PLAT',
    payload: index,
  }
);

export const _platAction = index => (
  {
    type: '_PLAT',
    payload: index,
  }
);

export const addAction = index => (
  {
    type: 'ADD',
    payload: index,
  }
);

export const addcommandeAction = index => (
  {
    type: 'ADD_COMMANDE',
    payload: index,
  }
);

export const commandeAction = index => (
  {
    type: 'COMMANDE',
    payload: index,
  }
);

export const profilAction = index => (
  {
    type: 'PROFIL',
    payload: index,
  }
);
export const setState = index => (
  {
    type: 'STATE',
    payload: index,
  }
);

export const notificationAction = index => (
  {
    type: 'NOTIFICATION',
    payload: index,
  }
);

export const notificationAddAction = index => (
  {
    type: 'NOTIFICATION_ADD',
    payload: index,
  }
);
